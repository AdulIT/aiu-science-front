version: '3.8'

services:
  front:
    restart: unless-stopped
    build:
      context: ./
    security_opt:
      - apparmor=unconfined
    expose:
      - "5173"
    ports:
      - "5173:5173"
    command: npm run dev
    networks:
      - compose_network

networks:
  compose_network:
    external: true
